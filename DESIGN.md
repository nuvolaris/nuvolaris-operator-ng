There is a module

A module has:

- name
- crd
- deployments


plugins/common
plugins/community
plugins/enterprise

deploy is a shared folder

the core is ... core
it reads the environment variables
some the configuration 